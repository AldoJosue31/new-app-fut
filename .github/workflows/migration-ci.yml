name: Migration safety net

on:
  pull_request:
    branches:
      - develop
      - main
  push:
    branches:
      - develop
      - main

permissions:
  contents: read

concurrency:
  group: migration-ci-${{ github.ref }}
  cancel-in-progress: true

env:
  NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: ci-anon-key
  NEXT_PUBLIC_SUPABASE_URL: http://127.0.0.1:54321

jobs:
  quality:
    name: Lint, tests and build
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install exact dependencies
        run: npm ci

      - name: Lint application
        run: npm run lint

      - name: Run Node tests
        run: npm run test:unit

      - name: Run Deno Edge tests
        run: npm run test:edge

      - name: Run API contracts
        run: npm run test:contract

      - name: Build production artifact
        run: npm run build

  browser:
    name: Browser smoke (${{ matrix.browser }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        browser:
          - chromium
          - webkit
          - firefox

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install exact dependencies
        run: npm ci

      - name: Build production artifact
        run: npm run build

      - name: Install browser runtime
        run: npx playwright install --with-deps ${{ matrix.browser }}

      - name: Run browser smoke
        run: npx playwright test --project=${{ matrix.browser }}

      - name: Upload browser diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-${{ matrix.browser }}
          path: |
            playwright-report
            test-results
          if-no-files-found: ignore
          retention-days: 7

  mobile:
    name: Browser smoke (mobile Chromium)
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install exact dependencies
        run: npm ci

      - name: Build production artifact
        run: npm run build

      - name: Install Chromium runtime
        run: npx playwright install --with-deps chromium

      - name: Run mobile smoke
        run: npx playwright test --project=mobile-chromium

      - name: Upload mobile diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-mobile-chromium
          path: |
            playwright-report
            test-results
          if-no-files-found: ignore
          retention-days: 7
